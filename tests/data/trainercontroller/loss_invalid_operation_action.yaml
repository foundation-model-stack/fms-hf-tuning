controller-metrics:
  - name: loss
    class: Loss
controllers:
  - name: loss-controller-invalid-operation-action
    triggers:
      - on_log
    rule:
      rule_str: loss < 1.0
    operations:
      - hfcontrols.missingaction