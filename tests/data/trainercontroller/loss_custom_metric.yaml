controller-metrics:
  - name: testflag
    class: CustomMetric
controllers:
  - name: loss-controller-custom-metric
    triggers:
      - on_log
    rule: 
      rule_str: testflag == True
    operations:
      - hfcontrols.should_training_stop