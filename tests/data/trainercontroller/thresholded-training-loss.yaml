controller-metrics:
  - name: training_loss_window
    class: HistoryBasedMetric
    arguments:
      window-size: 1
controllers:
  - name: stop-on-training-loss-not-decreasing
    triggers:
      - on_log
    rule: training_loss_window["loss"][-1] < 1
    operations:
      - hfcontrols.should_training_stop