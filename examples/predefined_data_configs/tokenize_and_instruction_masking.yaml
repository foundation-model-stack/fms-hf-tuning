dataloader:
    type: default
datasets:
  - name: text_dataset_input_output_masking
    data_paths:
      - "FILE_PATH"
    data_handlers:
      - name: tokenize_and_apply_instruction_masking
        arguments:
          remove_columns: all
          batched: false
          fn_kwargs:
            input_field: "INPUT"
            output_field: "OUTPUT"